// google integration

import React from 'react';

const google_module = () => (
    <div>Google Drive Module</div>
);

export default {
    props: {
        hasPanel: "true"
    },
    name: 'google_module',
}