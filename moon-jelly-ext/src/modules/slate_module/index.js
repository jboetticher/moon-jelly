// slate integration

import React from 'react';

const slate_module = () => (
    <div>Slate Module</div>
);

export default {
    props: {
        hasPanel: "true"
    },
    name: 'slate_module',
}